{
  "name": "deepseek-chatbot-netlify",
  "version": "1.0.0",
  "description": "DeepSeek AI Chatbot deployed on Netlify",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "netlify dev"
  },
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}